# 로컬 개발용 (.env로 복사 후 값 채우기)
# python -c "import secrets; print(secrets.token_urlsafe(50))" 로 키 생성

DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=True

# 운영 배포 시 (Railway 환경변수에 아래 항목 설정)
# DJANGO_SECRET_KEY=<강력한-랜덤-키>
# DJANGO_DEBUG=False
# DJANGO_ALLOWED_HOSTS=your-app.railway.app
